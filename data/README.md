# Calpus — source course data

Raw course material uploaded by the institution, organised by `<Department>-<Semester>-<Subject>/`.
Each folder holds the module PDFs + the official syllabus.

Calpus compiles these into the clean, watermarked textbooks in `../textbooks/`
(regenerate with `tools/build_textbook.py`). The web app serves those textbooks.
